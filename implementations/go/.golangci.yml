version: "2"

run:
  timeout: 5m

linters:
  # 기본 활성화 linter(errcheck, govet, ineffassign, staticcheck, unused)에
  # 명백히 유용한 항목만 추가한다. exhaustive/strict 규칙은 넣지 않는다.
  default: standard
  enable:
    - unconvert

formatters:
  enable:
    - gofmt
